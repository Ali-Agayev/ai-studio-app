# Example environment variables for backend (do NOT commit secrets into repo)
# Copy this file to .env locally for development, and set real secrets in Render dashboard for production.

# Database
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# JWT
JWT_SECRET=your_jwt_secret_here

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# OpenAI
OPENAI_API_KEY=your_openai_api_key

# Email (nodemailer) - optional
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password

# Payoneer
# Template must include {token} placeholder which will be replaced by server.
PAYONEER_CHECKOUT_URL_TEMPLATE=https://link.payoneer.com/Token?t={token}&src=wpl
# Or use base URL only and server will append ?t={token}
# PAYONEER_BASE_URL=https://link.payoneer.com/Token

# Webhook secret configured in Payoneer dashboard
PAYONEER_WEBHOOK_SECRET=replace_with_webhook_secret
